# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt
migration
migration-*
release.sh
AGENTS.md
TODO.md
dist/
.DS_Store

# Local dev workspace (relative paths, never committed); the release
# builds with GOWORK=off against the pinned go.mod versions.
go.work
go.work.sum
